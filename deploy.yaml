kind: Deployment
apiVersion: apps/v1
metadata:
  name: collateral-java
spec:
  replicas: 1
  selector:
    matchLabels:
      app: collateral-java-app
  template:
    metadata:
      labels:
        app: collateral-java-app
    spec:
      containers:
        - name: collateral-java
          image: collateral-java-image:latest
          ports:
            - name: http
              containerPort: 8081
              protocol: TCP
          resources:
            requests:
              memory: "300Mi"
              cpu: "50m"
---
kind: Service
apiVersion: v1
metadata:
  name: collateral-java-svc
spec:
  ports:
    - name: http-8081
      port: 8081
      protocol: TCP
      targetPort: 8081
  selector:
    app: collateral-java-app